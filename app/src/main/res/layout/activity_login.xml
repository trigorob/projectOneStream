<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".activity.LoginActivity">
    <Button
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:id="@+id/back"
        android:gravity="top"
        android:text="Back"
        android:onClick="goBack" />
    <Button
        android:id="@+id/spotifyLoginLauncherButton"
        android:layout_centerInParent="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="onSpotifyLoginButtonClicked"
        android:text="@string/spotify_login_button"/>
    <Button
        android:id="@+id/googleMusicLoginLauncherButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:onClick="onGoogleMusicLoginButtonClicked"
        android:text="Login to Google Music"
        android:layout_below="@+id/spotifyLoginLauncherButton"/>
    <Button
        android:id="@+id/oneStreamDomainLauncherButton"
        android:layout_centerInParent="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="onSpotifyLoginButtonClicked"
        android:text="@string/onestream_login_button"
        android:layout_below="@+id/googleMusicLoginLauncherButton"/>


</RelativeLayout>